services:
  postgres:
    image: postgres:16-alpine
    container_name: kicker-api-db
    environment:
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
      POSTGRES_DB: ${DB_NAME:-kicker_db}
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./internals/adapters/postgres/migrations:/docker-entrypoint-initdb.d
    networks:
      - kicker-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-postgres}"]
      interval: 10s
      timeout: 5s
      retries: 5

  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: kicker-api
    ports:
      - "${API_PORT:-8080}:8080"
    environment:
      DB_USER: ${DB_USER:-postgres}
      DB_PASSWORD: ${DB_PASSWORD:-postgres}
      DB_HOST: postgres
      DB_PORT: "5432"
      DB_NAME: ${DB_NAME:-kicker_db}
      API_PORT: ${API_PORT:-8080}
      ENV: ${ENV:-development}
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - kicker-network
    restart: unless-stopped

volumes:
  postgres_data:

networks:
  kicker-network:
    driver: bridge
